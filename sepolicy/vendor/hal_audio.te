hal_client_domain(hal_audio_default, hal_dms)
allow hal_audio_default sysfs:file rw_file_perms;
allow hal_audio_default sysfs:dir { open read };

# Dolby denial fix
allow hal_audio_default vendor_audio_prop:file { read open getattr };
allow hal_audio_default property_socket:sock_file write;